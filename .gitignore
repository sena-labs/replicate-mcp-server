node_modules/
dist/
*.log
.DS_Store
.env
.env.local

# Claude Code session files (may contain API keys / permissions)
.claude/

# Runtime artifacts
*.db
mcpo*.json
mcpo.err
mcpo.log
test-sdk.mjs
ruvector.db

# MCPB build output (manifest + build script are tracked; artifacts are not)
mcpb/build/
mcpb/*.mcpb

# Icon-generation exploration (the chosen assets/icon.png is tracked)
assets/candidates/
