# Copy to deploy/.env (gitignored) on the VPS and fill in.
# Public IPv4 of the host — Caddy binds ONLY this IP for 80/443.
PUBLIC_IP=178.105.2.218
# The public hostname Smithery will scan.
MCP_DOMAIN=replicate-mcp.sena-labs.dev
# Email for the Let's Encrypt account (expiry notices).
ACME_EMAIL=you@example.com
